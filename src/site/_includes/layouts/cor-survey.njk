---
layout: layouts/cor-base.njk
templateClass: tmpl-home
---

{{ content | safe }}

<cor-word-cloud class="cor-word-cloud"></cor-word-cloud>

<div>
  
  {% for item in survey %}
  
    <dt>{{item.question}}</dt>
      
      {% for response in item.responses %}
      
        <dd>
          

          {% if response.choices %}
            
            {# Start Multi bar graph #}
            
            {{response.text}}

            <multi-bar-chart>
              {% for choice in response.choices %}
                <multi-bar-item value="{{choice.value}}">{{choice.text}} <span>{{choice.value}}%</span></multi-bar-item>
              {% endfor %}
            </multi-bar-chart>

            {# End Multi bar graph #}

          {% else %}

            {# Start Simple bar graph #}
            {{response.text}} <span>{{response.value}}%</span>

            <bar-chart value="{{response.value}}"></bar-chart>

            {# End Simple bar graph #}
          {% endif %}
        </dd>
      
      {% endfor %}

      {# To delete later when style is applied #}
      <hr style="margin: 50px"/>
    
    {% endfor %}

    
  
</div>


