---
layout: layouts/base.njk
templateClass: tmpl-home
---
aa
<video
    id="my-video"
    class="video-js"
    controls
    preload="auto"
    width="640"
    height="264"
    poster="MY_VIDEO_POSTER.jpg"
    data-setup="{}"
  >
    <!-- <source src="{{ '/assets/videos/142nd-plenary-session-of-the-committee-of-the-regions-05022021.mp4' | url }}" type="video/mp4" /> -->
    <source src="{{ '/assets/videos/index.m3u8' | url }}" type="application/x-mpegURL" />
    <source src="MY_VIDEO.webm" type="video/webm" />
    <p class="vjs-no-js">
      To view this video please enable JavaScript, and consider upgrading to a
      web browser that
      <a href="https://videojs.com/html5-video-support/" target="_blank"
        >supports HTML5 video</a
      >
    </p>
  </video>

  <script>console.log("iojwdw")</script>

<script src="https://vjs.zencdn.net/7.10.2/video.min.js"></script>
 
<script>
var options = {};

var player = videojs('my-video', options, function onPlayerReady() {
  videojs.log('Your player is ready!');

  // In this context, `this` is the player that was created by Video.js.
  this.play();

  var track = new videojs.AudioTrack({
  id: 'my-spanish-audio-track',
  kind: 'translation',
  label: 'French',
  language: 'fre',
  enabled: true
});

  this.audioTracks().addTrack(track);


  debugger;



  // How about an event listener?
  this.on('ended', function() {
    videojs.log('Awww...over so soon?!');
  });
});
/*

console.log("111");
// Create a player.
var player = videojs('my-video');

console.log("qqqq",player);





// Create a track object.
var track = new videojs.AudioTrack({
  id: 'my-spanish-audio-track',
  kind: 'translation',
  label: 'French',
  language: 'fre',
  enabled: "true"
});

// Add the track to the player's audio track list.
player.audioTracks().addTrack(track);

// Get the current player's AudioTrackList object.
var audioTrackList = player.audioTracks();

audioTrackList[0].enabled = true;

console.log("audioTrackList[0].enabled")
// Listen to the "change" event.
audioTrackList.addEventListener('change', function() {

  // Log the currently enabled AudioTrack label.
  for (var i = 0; i < audioTrackList.length; i++) {
    var track = audioTrackList[i];
    
    if (track.enabled) {
      videojs.log(track.label);
      return;
    }
  }
});

*/
</script>
